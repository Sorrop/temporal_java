(ns temporal-java.core
  (:import (io.temporal.workflow
            Workflow)
           (io.temporal.activity
            ActivityOptions)
           (java.time Duration))
  (:gen-class))


(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
